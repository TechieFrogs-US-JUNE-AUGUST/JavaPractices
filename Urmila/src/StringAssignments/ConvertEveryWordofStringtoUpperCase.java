package StringAssignments;

public class ConvertEveryWordofStringtoUpperCase
{
    public static void main(String[] args)
    {
     
     /*   String str = "strings in java are not primitive type";//create a string
     String s1  = str.substring(0,1); // substring contains first letter of each word in string
     String s2  = str.substring(1,str.length());// substring contains remaining letters in string

     s1  = s1.toUpperCase();// method used for changing 1st letter to uppercase
     str = s1 + s2; //joining two substrings
     
     System.out.println("After changing :" + str);
     */
    String s1="this is java program using string";
    System.out.println(s1.toUpperCase()) ;
    }
    
}
